# المقال إلى كندل — الملخّص الشامل

_آخر تحديث: ٦ يوليو ٢٠٢٦_

هذا ملخّص غير تقنيّ لما بُني، والقرارات الكبرى وسوابقها، وما تحقّقتُ منه فعلًا،
وما يحتاج نقرتك أنت، وكيف تختبر، والخطوة التالية.

---

## ١. ما الذي بُني

أداة تحوّل أيّ مقال تقرأه إلى كتاب EPUB نظيف وترسله إلى كندل، بعربيّة سليمة من
اليمين لليسار، مع ترجمة اختيارية بالذكاء الاصطناعيّ. تتكوّن من قطعتين:

- **إضافة كروم** — تقرأ المقال داخل متصفّحك، وتبني ملف EPUB (مع الترجمة والخطّ
  العربيّ)، وتسلّمه لخدمة التوصيل. واجهتها عربيّة بالكامل باتجاه صحيح.
- **خدمة توصيل صغيرة** — برنامج صغير يتكلّم مع أمازون نيابةً عنك ليضع الملف في
  مكتبة كندل. تعمل على جهازك الآن، وعلى خادمك الدائم (VPS) لاحقًا.

**أهمّ ما يميّزها:** التوصيل يمرّ عبر **حساب أمازون نفسه** (تسجيل دخول مرّة واحدة)،
فلا بريد، ولا خطوة «اعتماد المُرسِل» التي تزعج مستخدمي كل الأدوات المنافسة.

---

## ٢. القرارات الكبرى وسوابقها

كل القرارات مسجّلة بالتفصيل في `docs/DECISIONS.md`. أهمّها:

1. **الصيغة EPUB3** لأنّ أمازون لم تعُد تقبل الصيغ القديمة (تحقّقت من ذلك، لا افتراض).
2. **بناء الملف داخل المتصفّح** لا على خادم — يتجاوز الحوائط المدفوعة ويحفظ الخصوصيّة.
3. **الترجمة عبر OpenRouter** (افتراضيًّا Claude) — التزامًا بقاعدتك المسجّلة: «أيّ نموذج
   خارجيّ يُوجَّه إلى OpenRouter، لا مفتاح Gemini». والقواعد العربيّة (فصحى وسطى، بلا
   تشكيل، منع «بل») مغروسة في تعليمات الترجمة.
4. **خطّ عربيّ مضمّن (Amiri)** لأنّ كندل بدونه يعرض مربّعات فارغة مكان الحروف.
5. **التحوّل الكبير — من البريد إلى تسجيل دخول أمازون (OAuth):** بعد أن جرّبنا البريد
   ورأيته متعبًا ومريبًا للمستخدم، بحثتُ في السوق فتبيّن أنّ كل المنافسين (Push to
   Kindle، KTool، Readwise، Instapaper) عالقون في البريد وخطوة الاعتماد، وأنّ طريق
   أمازون الرسميّ (تسجيل دخول) خالٍ منها. فحوّلتُ التوصيل إليه. **السابقة:** إذنك
   الصريح باستخدام واجهات غير رسميّة عند الحاجة، وتوجيهك أن أبني «تجربة المستخدم لا
   حلول المطوّر».
6. **تنفيذ هذا الطريق على الخادم لا في المتصفّح:** البحث كشف أنّ المتصفّح لا يستطيع
   نداء نقاط أمازون مباشرةً (حاجز CORS + توقيع تشفيريّ غير قياسيّ لا يدعمه المتصفّح).
   الخادم (Node) يفعلها بدقّة. تجربة المستخدم لم تتغيّر؛ التنفيذ صار موثوقًا.

---

## ٣. المال والحسابات الخارجيّة (سجلّ صريح)

- **لم يُنشأ أيّ حساب مدفوع** طوال العمل (لا OpenRouter، لا استضافة، لا خدمة إرسال).
- **بريد فعليّ أُرسل مرّة واحدة:** أثناء اختبار طريق البريد، خرجت رسالة تجريبيّة من
  بريدك `ajarallah93@gmail.com` إلى بريد كندلك عبر SMTP (قبِلها جيميل). لم تصل إلى
  كندل على الأرجح لأنّ المُرسِل لم يُعتمَد في أمازون — وهي خطوة تجاوزناها كليًّا
  بالتحوّل إلى تسجيل الدخول.
- **كلمة مرور تطبيق جيميل** التي زوّدتني بها محفوظة محلّيًّا في `server/.env`
  (مُستثنى من git، لا يُرفع). إن قرّرت عدم استخدام طريق البريد، **يمكنك إلغاؤها** من
  إعدادات حساب جوجل بأمان.
- **لم يُضبط مفتاح OpenRouter** بعد؛ الترجمة تنتظر مفتاحك.

---

## ٤. ما تحقّقتُ منه فعلًا ✓ مقابل ما يحتاج نقرتك

**تحقّقتُ منه بنفسي (يعمل):**
- بناء EPUB سليم — فحصتُ بنية الملف الناتج فعليًّا.
- الخادم يقلع وكل نقاطه تستجيب صحيحًا.
- **خوارزميّة التوقيع مع أمازون متحقَّقة ذاتيًّا** عبر اختبارات آليّة (٣/٣ تنجح):
  التوقيع الناتج يُفكّ ويطابق البصمة المطلوبة بايتًا ببايت.
- بناء جملة كل ملفّات الإضافة سليم، وحزمة النشر تُبنى نظيفة (٢٠ ملفًّا).
- الواجهة عربيّة RTL بتصميم مضبوط.

**يحتاج نقرتك (لا أملك حسابك فلم أستطع اختباره):**
- **ربط حساب كندل:** تسجيل دخول أمازون مرّة واحدة من الإعدادات.
- **أمران لا يمكن التحقّق منهما إلّا بحساب حيّ:** هل تقبل أمازون التوقيع المُعاد
  إنتاجه، وهل تقبل صيغة EPUB في الرفع. لهذا أبقيتُ **طريق البريد خطَّ رجوع مثبتًا**
  حتى لا تبقى بلا وسيلة إرسال إن ظهر عائق.
- تحميل الإضافة في كروم لديك (لا يمكنني فعله نيابةً عنك بثقة).

---

## ٥. كيف تختبر (١٠ دقائق)

```
١) شغّل خدمة التوصيل:
   cd server && npm install && npm start   → http://localhost:8787

٢) حمّل الإضافة:
   chrome://extensions ← Developer mode ← Load unpacked ← اختر مجلد extension/

٣) اربط كندل (مرّة واحدة):
   الإضافة ← الترس (الإعدادات) ← «ربط حساب كندل» ← سجّل الدخول في أمازون
   → يُكمَل الربط تلقائيًا ويعرض أجهزتك.

٤) أرسل:
   افتح مقالًا ← أيقونة الإضافة ← «إرسال إلى كندل».
   أو «تنزيل EPUB» ليحفظ الملف بلا إرسال (يعمل بلا ربط).

٥) الترجمة (اختياري):
   ضع مفتاح OpenRouter في الإعدادات، فعّل الترجمة، اختر العربية.
```

إن نجح الربط والإرسال، فالطريق الجديد يعمل من طرفه إلى طرفه. إن ظهر خطأ في مرحلة
أمازون، نلجأ لطريق البريد المثبت، أو نعالج الفرق (غالبًا صيغة الرفع).

---

## ٦. الخطوة التالية المقترحة

1. **جرّب الربط والإرسال** كما في القسم ٥، وأخبرني بالنتيجة — هذه اللحظة الوحيدة
   التي أحتاج فيها حسابك، وبعدها نعرف يقينًا أنّ الطريق الأمثل يعمل.
2. إن عمل: **ننشر خدمة التوصيل على VPS هوستنجر** لتعمل بلا تشغيل حاسوبك، ثم نرفع
   الإضافة إلى متجر كروم (أصول القائمة والخصوصيّة جاهزة في `store/`).
3. إن تعثّر شيء في مرحلة أمازون: نعالجه، وطريق البريد يبقى شبكة أمان.

الأداة الآن مبنيّة بالكامل ومتماسكة؛ ما تبقّى هو تسجيل دخولك مرّة، ثم النشر.
