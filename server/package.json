{
  "name": "article-to-kindle-tests",
  "version": "0.1.0",
  "description": "Test + lint harness for the Article to Kindle («جسر») extension. The product is a fully serverless MV3 extension under ../extension; this folder only holds Node tests that import the extension modules, the icon generator, and dev tooling.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "cd .. && eslint extension/src server/test server/gen-icons.mjs",
    "icons": "node gen-icons.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "jsdom": "^29.1.1",
    "sharp": "^0.35.3"
  }
}
