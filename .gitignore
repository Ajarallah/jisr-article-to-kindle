node_modules/
.DS_Store
*.log
dist/
.env
.stk-credentials.json
*.epub

# Bundled translation key. Ships inside the unpacked extension folder, never in
# git — a key pushed to a public repo is scraped within minutes and stays in the
# history forever. Copy secrets.example.js to secrets.js and fill it in.
extension/src/secrets.js

# Internal status pages kept locally, not part of the published project.
PROGRESS.html
SUMMARY.md
HANDOFF.md
plans/
.claude/
.code-review-graph/
